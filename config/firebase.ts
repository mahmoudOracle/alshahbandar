
import { FirebaseOptions } from 'firebase/app';

/**
 * This file is deprecated. The Firebase configuration has been moved directly into
 * `services/firebase.ts` to prevent module resolution issues and improve startup reliability.
 */
export const firebaseConfig: FirebaseOptions | undefined = undefined;
