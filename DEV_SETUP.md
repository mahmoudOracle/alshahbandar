# Development setup

1. Copy `.env.local.example` to `.env.local` and fill in your own values (especially `VITE_COMPANY_ID=uv9acIebvvNgx9ftSnPh`).
2. Restart the dev server (`npm run dev`) whenever you change `.env.local` so Vite re-reads the updated env vars.
